---
sidebar_position: 7
title: "sphere"
description: "Creates a full sphere with the given radius."
---

# sphere()

Creates a full sphere with the given radius.

**Returns**: [**`Transformable`**](/docs/api/types/transformable)

## Signatures

```ts
sphere(radius: number): Transformable
```

Creates a full sphere with the given radius.

| Parameter | Type | Description |
|-----------|------|-------------|
| `radius` | number | The sphere radius |

---

```ts
sphere(radius: number, angle: number): Transformable
```

Creates a partial sphere with the given radius and sweep angle.

| Parameter | Type | Description |
|-----------|------|-------------|
| `radius` | number | The sphere radius |
| `angle` | number | The sweep angle in degrees |
