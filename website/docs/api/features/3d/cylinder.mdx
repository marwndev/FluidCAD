---
sidebar_position: 8
title: "cylinder"
description: "Creates a cylinder with the given radius and height."
---

# cylinder()

Creates a cylinder with the given radius and height.

**Returns**: [**`Transformable`**](/docs/api/types/transformable)

## Signatures

```ts
cylinder(radius: number, height: number): Transformable
```

Creates a cylinder with the given radius and height.

| Parameter | Type | Description |
|-----------|------|-------------|
| `radius` | number | The cylinder radius |
| `height` | number | The cylinder height |
